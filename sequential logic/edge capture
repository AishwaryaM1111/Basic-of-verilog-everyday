module top_module (
    input clk,
    input reset,
    input [31:0] in,
    output [31:0] out
);
    reg [31:0]pre_in;
    always @(posedge clk) begin
        pre_in<=in;
        if (reset)
            out<=0;
        else begin
            out<=out|(~in & pre_in);
        end 
    end
endmodule
